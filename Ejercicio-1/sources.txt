
-create enviroment
	pip install virtualenv
	-create: virtualenv -p python3 <name env>
	-open: <name env>\scripts\activate (windows) 

-modules
	pip install Flask
	pip install Flask-Script
	pip install Flask-SQLAlchemy
	pip install Flask-Login
